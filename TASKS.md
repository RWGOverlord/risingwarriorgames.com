

- [ ] Update Late Pledge Redirect URL in latepledge.html
      - Locate the redirect URL in `latepledge.html` (likely in a `<meta http-equiv="refresh">` tag, `window.location` JS call, or `<a>` href)
      - Replace the existing Kickstarter URL with `https://zombie-overlord.backerkit.com/hosted_preorders`
      - Verify all instances of the old Kickstarter URL are replaced if referenced more than once in the file
      - Test the redirect in browser to confirm it routes correctly to the BackerKit pre-orders page

- [ ] Update Late Pledge Redirect URL in latepledge.html
      - Locate the redirect URL in `latepledge.html` (likely in a `<meta http-equiv="refresh">` tag, `window.location` JS call, or `<a>` href)
      - Replace the existing Kickstarter URL with `https://zombie-overlord.backerkit.com/hosted_preorders`
      - Verify all instances of the old Kickstarter URL are replaced if referenced more than once in the file
      - Test the redirect in browser to confirm it routes correctly to the BackerKit pre-orders page
